(()=>{
'use strict';
window.IVTC=window.IVTC||{};
const enc=new TextEncoder();
const b64=b=>btoa(String.fromCharCode(...new Uint8Array(b)));
function requireState(){const s=window.IVTC.firebase?._state;if(!s?.user||!s.db||!s.api)throw new Error('Sign in first.');return s;}
async function encryptConnectivityPayload(value){const key=await crypto.subtle.generateKey({name:'AES-GCM',length:256},true,['encrypt','decrypt']);const iv=crypto.getRandomValues(new Uint8Array(12));const cipher=await crypto.subtle.encrypt({name:'AES-GCM',iv},key,enc.encode(JSON.stringify(value)));return {algorithm:'AES-256-GCM',iv:b64(iv),ciphertext:b64(cipher),testOnly:true};}
async function createTrip({label='New trip',startDate=null,endDate=null,travelers=1,status='active',source='manual',packagedVersion=null,itinerary=null,id=null}={}){const s=requireState(),tripRef=id?s.api.doc(s.db,'trips',id):s.api.doc(s.api.collection(s.db,'trips'));const trip={schema:2,label:label.trim()||'New trip',startDate:startDate||null,endDate:endDate||null,travelers:Number(travelers)||1,ownerUid:s.user.uid,memberUids:[s.user.uid],roles:{[s.user.uid]:'owner'},createdAt:s.api.serverTimestamp(),updatedAt:s.api.serverTimestamp(),status,source};if(packagedVersion)trip.packagedVersion=packagedVersion;if(itinerary)trip.itinerary=itinerary;const write=s.api.setDoc(tripRef,trip);const outcome=await Promise.race([write.then(()=>({synced:true})).catch(error=>({error})),new Promise(resolve=>setTimeout(()=>resolve({queued:true}),4000))]);if(outcome.error)throw outcome.error;return {id:tripRef.id,trip:{id:tripRef.id,...trip},queued:!!outcome.queued};}
async function listTrips({includeArchived=true,timeoutMs=6000}={}){const s=requireState();const q=s.api.query(s.api.collection(s.db,'trips'),s.api.where('memberUids','array-contains',s.user.uid));let cached=null;try{cached=await s.api.getDocsFromCache(q);}catch{}const server=await Promise.race([s.api.getDocs(q).then(snap=>({snap})).catch(error=>({error})),new Promise(resolve=>setTimeout(()=>resolve({timeout:true}),timeoutMs))]);const snap=server.snap||cached;if(!snap){if(server.error)throw server.error;throw new Error('Cloud trip lookup timed out and no offline copy is available.');}const items=snap.docs.map(d=>({id:d.id,...d.data()})).filter(t=>includeArchived||t.status!=='archived').sort((a,b)=>{const av=a.updatedAt?.seconds||0,bv=b.updatedAt?.seconds||0;return bv-av;});Object.defineProperty(items,'fromCache',{value:!server.snap,enumerable:false});return items;}
async function bootstrapPackagedTrip(packaged){if(!packaged||!Array.isArray(packaged.stages))throw new Error('The packaged itinerary could not be read.');const existing=await listTrips();if(existing.length)return {created:false,trip:existing[0]};const travelerCount=String(packaged.travelers||'').split(/\s*(?:&|,|and)\s*/i).filter(Boolean).length||1;const created=await createTrip({id:`istanbul-viking-2026-${requireState().user.uid}`,label:'Istanbul · Viking · Venice & Northern Italy 2026',startDate:packaged.start||'2026-08-13',endDate:'2026-09-13',travelers:travelerCount,status:'active',source:'packaged-bootstrap',packagedVersion:packaged.version||null,itinerary:{title:packaged.title||null,subtitle:packaged.subtitle||null,ship:packaged.ship||null,stateroom:packaged.stateroom||null,hotel:packaged.hotel||null,stages:packaged.stages}});const trip=created.trip;selectTrip(trip);return {created:true,trip,queued:created.queued};}
async function updateTrip(id,changes){const s=requireState();if(!id)throw new Error('Trip ID is required.');await s.api.updateDoc(s.api.doc(s.db,'trips',id),{...changes,updatedAt:s.api.serverTimestamp()});}
async function deleteTrip(id){const s=requireState();if(!id)throw new Error('Trip ID is required.');await s.api.deleteDoc(s.api.doc(s.db,'trips',id));if(localStorage.getItem('ivtc.activeTripId')===id){localStorage.removeItem('ivtc.activeTripId');localStorage.removeItem('ivtc.activeTripLabel');}}
async function duplicateTrip(id){const source=(await listTrips()).find(t=>t.id===id);if(!source)throw new Error('Trip not found.');return createTrip({label:`${source.label||'Trip'} copy`,startDate:source.startDate||null,endDate:source.endDate||null,travelers:source.travelers||1,status:'active',source:'duplicate',itinerary:source.itinerary||null});}
function selectTrip(trip){if(!trip?.id)throw new Error('Trip is required.');localStorage.setItem('ivtc.activeTripId',trip.id);localStorage.setItem('ivtc.activeTripLabel',trip.label||'Selected trip');window.dispatchEvent(new CustomEvent('ivtc:trip-selected',{detail:trip}));}
function selectedTrip(){return {id:localStorage.getItem('ivtc.activeTripId'),label:localStorage.getItem('ivtc.activeTripLabel')};}
window.IVTC.tripCloud=Object.freeze({createTrip,listTrips,bootstrapPackagedTrip,updateTrip,deleteTrip,duplicateTrip,selectTrip,selectedTrip});
})();
